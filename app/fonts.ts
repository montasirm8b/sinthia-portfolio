import { Abril_Fatface, Mulish } from "next/font/google";

export const mulishBold = Mulish({
    subsets: ["latin"],
    weight: "800"
});

export const mulishSemibold = Mulish({
    subsets: ["latin"],
    weight: "700",
});

export const mulish = Mulish({
    subsets: ["latin"],
    weight: "400",
});


export const abrilFatface = Abril_Fatface({
    subsets: ["latin"],
    weight: "400",
})